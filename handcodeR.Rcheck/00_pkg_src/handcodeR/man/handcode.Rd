% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handcode.R
\name{handcode}
\alias{handcode}
\title{Categorial Text Annotation}
\usage{
handcode(
  data,
  ...,
  start = "first_empty",
  randomize = FALSE,
  context = FALSE,
  missing = c("Not applicable"),
  pre = NULL,
  post = NULL,
  comparison = NULL,
  pre_comparison = NULL,
  post_comparison = NULL,
  autosave = FALSE,
  add_notes = FALSE,
  enable_numeric = FALSE
)
}
\arguments{
\item{data}{A character vector of texts, or a data frame with a
\code{texts} column (and optionally pre-existing classification
columns to resume coding).}

\item{...}{Named character vectors defining classification variables.
Each name becomes a variable, each vector its category levels.}

\item{start}{Either \code{"first_empty"} (default) or an integer index
indicating which row to start coding at.}

\item{randomize}{Logical. If \code{TRUE}, randomize text order. Default \code{FALSE}.}

\item{context}{Logical. If \code{TRUE}, show preceding/following texts as
context. Default \code{FALSE}.}

\item{missing}{Character vector of labels for missing/not-applicable
values. Default \code{c("Not applicable")}.}

\item{pre}{Optional character vector of texts to prepend as context
(one per row).}

\item{post}{Optional character vector of texts to append as context
(one per row).}

\item{comparison}{Optional character vector for paired-text comparison
workflows.}

\item{pre_comparison}{Optional context-before vector for the comparison
text.}

\item{post_comparison}{Optional context-after vector for the comparison
text.}

\item{autosave}{Logical. If \code{TRUE}, periodically save progress to
disk after the user confirms a save location. Requires an interactive
session. Default \code{FALSE}.}

\item{add_notes}{Logical. If \code{TRUE}, show a free-text notes input
in the UI. Default \code{FALSE}.}

\item{enable_numeric}{Logical. If \code{TRUE}, enable numeric keyboard
shortcuts for category selection. Default \code{FALSE}.}
}
\value{
A data frame containing the original texts plus one column per
  classification variable. Returns \code{invisible(NULL)} if the user
  cancels the autosave or recovery setup.
}
\description{
Launch a Shiny app for hand-coding texts into one or more categorial
classification variables. Each variable is defined by name and its
set of allowed categories passed via \code{...}.
}
\examples{
\dontrun{
  texts <- c("I love this product", "Worst purchase ever", "It's okay")
  result <- handcode(
    texts,
    sentiment = c("positive", "neutral", "negative")
  )
}

}
\seealso{
\code{\link{handcode_binary}} for two-choice annotation.
}
