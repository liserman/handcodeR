context("check-handcode")
library(testthat)
library(handcodeR)

# Check stop if missing shiny installation


# Check stop if data is not dataframe


# Check stop if data is not in correct format


# Check stop if there are not enough categories to code


# Check stop if start is wrong format


# Check stop if start is of length > 1


# Check stop if newname is wrong format


# Check stop if newname is of length > 1
